
let arr =["string", 0 , true]
console.log(arr.slice(0, 1))//to print only 1st value
// 1st parameter is includes, 2nd parameter excludes - it excludes the index of given parameter
console.log(arr.slice(0))// if we pass only one parameter it prints the array from that index
//The string indexOf() method returns the index of the first occurence of the substring in a string.
let str = 'i am ayesha khatoon'
var substr = "n"
const index = str.indexOf('kha')
var result = str.lastIndexOf(substr);
console.log(index);
console.log(result)